st - simple terminal
--------------------
st is a simple terminal emulator for X which sucks less.


Requirements
------------
In order to build st you need the Xlib header files.


Installation
------------
Edit config.mk to match your local setup (st is installed into
the /usr/local namespace by default).

Afterwards enter the following command to build and install st (if
necessary as root):

    make clean install


Running st
----------
If you did not install st with make clean install, you must compile
the st terminfo entry with the following command:

    tic -sx st.info

See the man page for additional details.

Credits
-------
Based on Aurélien APTEL <aurelien dot aptel at gmail dot com> bt source code.

Version
-------
0.8.2

Patches
-------
st-alpha-0.8.2.diff
st-xresources-20190105-3be4cf1.diff
st-scrollback-0.8.2.diff
st-scrollback-mouse-0.8.2.diff
st-scrollback-mouse-altscreen-e23acb9.diff
st-externalpipe-0.8.2.diff

Key - Bindings
--------------
Zoom            - ctrl + plus, minus, equel
Scollback       - shift + page_up, page_down (mouse scroll)
External Pipe   - alt + l (select url with rofi to open in browser)
                  alt + y (select url with rofi to copy to xclip)
                  alt + o (select command with rofi to copy output to xclip)
